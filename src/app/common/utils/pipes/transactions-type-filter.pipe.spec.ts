import { TransactionsTypeFilterPipe } from './transactions-type-filter.pipe';

describe('TransactionsTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionsTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
